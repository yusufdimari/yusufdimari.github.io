export enum screens {
  About = "About",
  Portfolio = "Portfolio",
  Resume = "Resume",
  // Contact = "Contact",
  // Blog = "Blog",
}

export enum ProjectCategories {
  all = "all",
  web = "web",
  mobile = "mobile",
  backend = "backend",
}

export const isBigScreen = window.innerWidth >= 1024;
